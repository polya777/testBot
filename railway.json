{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "curl -fsSL https://mise.jdx.dev/install.sh | sh && export PATH=\"/root/.local/bin:$PATH\" && export MISE_NODE_MIRROR=https://mirrors.cloud.tencent.com/nodejs-release/ && mise install java@21 maven && mvn clean package -DskipTests"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ALWAYS",
    "sleepApplication": false,
    "startCommand": "java -Xmx512m -jar target/*.jar",
    "healthcheckPath": "/actuator/health",
    "healthcheckTimeout": 60
  },
  "environments": {
    "production": {
      "deploy": {
        "numReplicas": 2
      }
    }
  }
}